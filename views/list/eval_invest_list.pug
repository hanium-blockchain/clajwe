extends ../layout

block content
  table.table.listTable
    thead.thead-dark
      tr
        each item in list
          th(scope='col')= item
    tbody
      if check === 'invest'
        each i, idx in invest
          tr
            th(rowspan=2, scope='row')= idx+1
            td= i.category
            td= i.title
            td= i.register
            td= i.D_day
          tr
            td(colspan=4)= `가치 : ${i.value}`
      else if check === 'eval'
        each e, idx in eval
          tr
            th(scope='row')= idx+1
            td= e.category
            td= e.title
            td= e.register
            td= e.date
            td 
              a.nav-link(href='../evaluations/detail') 평가하기

